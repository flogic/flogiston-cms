%table
  - @pages.each do |page|
    %tr
      %td= page.handle
      %td= page.title
      %td= link_to 'show', admin_page_path(page)
      %td= link_to 'edit', edit_admin_page_path(page)
      %td= link_to 'delete', admin_page_path(page), :confirm => 'Are you sure?', :method => :delete
